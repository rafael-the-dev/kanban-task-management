import classNames from "classnames";

import classes from "./styles.module.css";

const Main = ({ children }) => {

    return (
        <main>
            { children }
        </main>
    );
};

export default Main;